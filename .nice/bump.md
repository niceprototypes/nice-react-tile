[2026-05-20 15:08] major: `mode` prop now wraps Tile in `<Mode name={mode}>` instead of pinning Tile's own styled-component to that mode in isolation. Behavior change: descendants of a mode-pinned Tile now inherit the pin via the data-theme cascade — including nested Typography, Icon, etc. that don't explicitly set their own mode. This matches the long-standing user expectation; previously consumers had to set `mode` on each child too. Children that set their own `mode` continue to override.
[2026-05-19 12:00] patch: Update consumer call sites to the unified getToken from nice-react-styles (was getReactToken). Internal-only rename — Tile's public props are unchanged. Comments updated to reference nice-styles' renamed CSS artifact (tokens.css).

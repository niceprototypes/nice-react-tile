[2026-05-23 21:05] patch: Remove broken `test: jest` script (no jest config). Lint kept (`.eslintrc` exists). Priority 1.5 normalization.
[2026-05-23 20:07] patch: Drop unused `rollup-plugin-peer-deps-external` devDependency — externalization is now handled by `nice-configuration/rollup`'s built-in peerDependencies reader. No build-output change.
[2026-05-23 09:00] major: Promote backgroundColor, foregroundColor, backgroundSize props to token types. Consumer-facing types narrow from `string` to `BackgroundColorType` / `ForegroundColorType` / `BackgroundSizeType` (re-exported as `TileBackgroundColorType` / `TileForegroundColorType` / `TileBackgroundSizeType`). Raw CSS color strings or arbitrary background-size values are no longer accepted — use a token variant (`"base"`, `"dark"`, etc.) or `backgroundImage` for gradients/URLs. Internal styles updated to resolve via getToken. Closes action item 1 of manifest/.nice/reports/audit/token-bindings.md.
[2026-05-20 15:08] major: `mode` prop now wraps Tile in `<Mode name={mode}>` instead of pinning Tile's own styled-component to that mode in isolation. Behavior change: descendants of a mode-pinned Tile now inherit the pin via the data-theme cascade — including nested Typography, Icon, etc. that don't explicitly set their own mode. This matches the long-standing user expectation; previously consumers had to set `mode` on each child too. Children that set their own `mode` continue to override.
[2026-05-19 12:00] patch: Update consumer call sites to the unified getToken from nice-react-styles (was getReactToken). Internal-only rename — Tile's public props are unchanged. Comments updated to reference nice-styles' renamed CSS artifact (tokens.css).

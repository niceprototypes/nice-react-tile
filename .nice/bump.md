[2026-05-24 06:30] major: Rename `foregroundColor` prop to `color` (align with CSS standards + Icon/Typography convention). `TileProps.foregroundColor` → `TileProps.color`. Type `TileForegroundColorType` → `TileColorType`. Namespace `TileTypes.ForegroundColor` → `TileTypes.Color`. Internal styled-components transient `$foregroundColor` → `$color`. Consumers using `<Tile foregroundColor="...">` must rename to `<Tile color="...">`.
